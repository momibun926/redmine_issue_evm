ja:
  activerecord:
    models:
      evmsetting: EVN設定
    attributes:
      evmsetting:
        etc_method: ETCの計算方法
        basis_hours: 一日の労働時間
        threshold_spi: SPIの閾値
        threshold_cpi: CPIの閾値
        threshold_cr:  CRの閾値
        region: 休日の地域
