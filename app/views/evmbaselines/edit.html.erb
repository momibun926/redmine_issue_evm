<h2><%= l(:label_baseline_edit) %></h2>
<%= labelled_form_for @evm_baselines, :url => {:action => 'update',method: :get} do |f| %>
  <p><%= f.text_field :subject, size: 60, required: true %></p>
  <p><%= f.text_field :description, size: 150 %></p>
  <%= submit_tag l(:button_edit) %>
<% end %>
