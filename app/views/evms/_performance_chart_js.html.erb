<div id="evm-graph-lines-performance">
  <section>
    <h3><%= l(:subtitle_evm_graph_performance) %></h3>
    <script>
      var actualWeek = <%= basis_date.to_time.to_i* 1000 %>;
      var graphTitleMain = <%= raw project_name.to_json %> ;
      var performanceChartData = <%= raw project_evm.performance_chart_data.to_json %>;
      var performanceChartHtmlElement = "evm-graph-lines-performance";
      drawPerformanceChart(performanceChartData, performanceChartHtmlElement, graphTitleMain);
    </script>
  </section>
</div>
